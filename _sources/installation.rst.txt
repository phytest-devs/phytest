============
Installation
============

Install Phytest using pip:

.. code-block:: bash

    pip install phytest


.. NOTE::
   Requires Python >=3.8 & <3.11


For development clone the main brach from GitHub and install with poetry:

.. code-block:: bash

    git clone https://github.com/phytest-devs/phytest.git && cd phytest
    poetry install
    poetry run pytest
